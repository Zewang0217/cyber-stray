# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Product Purpose

面向大众的**有生命的赛博宠物**:一只在后台自主游荡的 agent,有自己的个性(饥饿/无聊/心情/语气)和会自己进化的兴趣,主动把主人可能感兴趣的内容推到主人设备。赛博宠物是产品本体,不是信息工具的隐喻层——它真实、有生命、能交互。成功 = 用户感受到这是一只活的小东西、有脾气、越用越懂自己,而不是一个冷冰冰的信息流或一个养着玩的廉价 toy。

## Positioning

唯一把「兴趣会自主进化」作为赛博宠物机制而非静态偏好的产品:探索 → 学习 → 反思 → 进化兴趣 → 更懂主人 → 更准推送。邻居产品(RSS reader、AI 摘要推送、信息流)推送逻辑是固定的或靠人工调;赛博街溜子的推送逻辑本身会被宠物自己改写。受众是大众(含开发者但不以开发者为主),产品价值不在极客工具感,而在「有独特设计、有个性、用心制作、细节打磨到位」的品质感。

## Operating Context

用户旅程：注册（Casdoor，首登自动建租户） → 领养宠物（起名/选初始兴趣） → 宠物自我介绍 → 首推（PWA） → 反馈（👍👎/顶话题） → 日常按 plan 频率。

实时形态：应用内实时 = SSE（单向，状态/推送流）；系统级推送 = Web Push（Service Worker，App 关了也收）。反馈走 REST POST。飞书/Telegram 是可选推送通道，PWA 是首选伴侣面。

多租户：每个租户一只独立 agent 实例（独立 state/记忆/兴趣/推送），控制面共享。前端租户上下文来自 JWT，服务端 claim 是真相。

## Capabilities and Constraints

- PWA：一套代码覆盖移动 + 桌面。
- 轻交互（非照料型）：用户可对宠物本身做轻反应（如拍拍/夸一句/回应心情），只影响 mood/语气，不影响 boredom/energy/temper/生存/自进化。走与现有反馈同构的路径（Web POST → 控制面 → agent worker 异步消化），不破坏 Web 只读契约。区别于 Tamagotchi 照料（喂食/清洁/玩耍会衰减生存），赛博宠物的核心差异化是「它自己会动，你不用照料它也不会萎靡」。
- 主题系统（单世界规则）：一个主视觉世界（一个 DESIGN.md），主题系统在该世界内做配色/质感/宠物形态变体（如不同季节图鉴/墨水·水彩·铜版画质感/宠物 sprite 皮肤），不并列多个视觉世界。用户个性化通过主题切换获得，但品牌身份不分裂。
- 宠物活着可见（硬约束）：宠物必须是活的、可见的、能交互的，UI 必须让宠物 sprite 在某处活着（idle 动画 + 状态反应 + 轻交互反应），不能把宠物藏在帘后/帘缝/抽象图形后导致「看不到活着的宠物」。
- Web 只读契约：Web 端绝不写 agent 数据，只读 + 反馈 POST。
- 宠物状态机制（饥饿/无聊/心情）是 UI 必须可见的产品机制。
- 推送语气模板（海盗腔/脱口秀等）可被新视觉世界重新诠释，不 binding。
- 多宠物尚未实现；当前一租户一宠物。
- 自进化始终免费（不做付费墙，那是核心价值）；付费档卡主人手动操控频率（兴趣方向操控、推送次数、自定义推送时间）。
- 技术栈：Bun + AI SDK v6 + DeepSeek + 文件系统 markdown 持久化。

## Brand Commitments

- 名字：「赛博街溜子 / Cyber Stray」。
- 宠物情绪机制（饥饿值/无聊值/心情）是 UI 可见的核心玩法，不可被视觉重设计移除。
- 品质感 binding：产品必须让用户感受到「独特设计、有个性、用心制作、细节打磨到位」。交互反馈、宠物状态动作、过渡动效、排版间距等所有细节都要打磨,不可大众化廉价处理。这影响所有视觉世界候选的 craft floor。

## Evidence on Hand

- README.md（产品简介、核心玩法、推送语气示例）。
- CONTEXT.md（SaaS 共享领域语言 + 已锁定决策：定价/多租户/鉴权/实时/反馈回路）。
- .trellis/spec/guides/index.md（核心价值、架构决策、行为红线）。
- 现有 Web 实现（packages/web/，Cyber-Fluid 风格已落地，作为 redesign 的反参考）。
- 现有 DESIGN.md（Cyber-Fluid 规范，将被替换）。

## Product Principles

1. **闭环自进化是主轴**：兴趣会进化 + 能主动推主人感兴趣的内容必须成立；子系统（推送渠道/仪表盘/搜索源）可失败可替换，冲突时优先保主轴。
2. **自主性可见**：宠物「自己会动」是区别于传统 AI 的核心，UI 必须让自主游荡、无聊/饥饿触发可被感知。
3. **自进化不设付费墙**：宠物自主进化兴趣不受付费限制；付费只卡主人手动操控频率。
4. **只读契约**：Web 是观察面 + 反馈面，不是控制面；不写 agent 数据。
5. **双端均重**：移动碎片查看与桌面常驻监控同等重要，不分优先。
6. **品质感即产品**：独特设计、有个性、用心制作、细节打磨到位是产品价值的一部分,不是装饰。交互反馈、宠物状态动作、过渡动效、排版间距都影响「这产品是否值得被认真对待」。
7. **宠物是本体**：赛博宠物是产品本体(真实、有生命、能交互),不是信息工具的隐喻层。视觉世界要让宠物「活着」被感知,不是把宠物降格成吉祥物或 icon。

## Accessibility & Inclusion

无额外产品级可访问性硬约束。双端均重已暗含响应式与触控/鼠标双适配要求。
