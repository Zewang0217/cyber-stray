---
phase: 01-记忆基础设施
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/llm/stats.ts
  - src/llm/stats.test.ts
  - src/agent/react.ts
  - src/agent/react.test.ts
  - src/types.ts
  - src/config.ts
  - data/agent-config.json
autonomous: true
requirements: [MEM-03, MEM-04]
tags: [react-loop, llm-stats, speak, error-handling, mvp]

must_haves:
  truths:
    - "空游荡（spokeTimes===0）不再触发强制 speak nonsense 兜底推送（react.ts:223-229 整块删除）"
    - "LLM 调用统计反映真实步数：多步 ReAct loop 后 getLLMStats().calls > 1（不再恒 1）"
    - "onStepEnd 回调内计数逻辑用 try/catch 自愈，回调内异常不阻断主流程（防 SDK 静默吞错）"
    - "generateText 整体失败时记 ERROR + 按 config.generateTextMaxRetries 重试（默认 1 次）；该 config 键由本 plan 自包含写入（types.ts/config.ts/agent-config.json），无跨 wave 依赖（W1）"
  artifacts:
    - path: "src/llm/stats.ts"
      provides: "StepRecord + recordStep + getLLMStats + resetLLMStats（onStepEnd 驱动的按步累加，替换 startLLMCall/endLLMCall 包装）"
      exports: ["StepRecord", "recordStep", "getLLMStats", "resetLLMStats"]
      min_lines: 40
    - path: "src/agent/react.ts"
      provides: "runAgentLoop 改造：删强制 speak 块 + onStepEnd 按步计数 + generateText 重试"
      contains: "onStepEnd"
    - path: "src/llm/stats.test.ts"
      provides: "MEM-04 Wave 0 测试：recordStep 累加 / getLLMStats 聚合 / resetLLMStats"
      min_lines: 40
    - path: "src/agent/react.test.ts"
      provides: "扩展：按步计数 calls>1 + 空游荡不调 speak + 失败重试"
      min_lines: 80
  key_links:
    - from: "src/agent/react.ts (generateText onStepEnd)"
      to: "src/llm/stats.ts (recordStep)"
      via: "AI SDK v6 生命周期回调按步累加统计"
      pattern: "recordStep|onStepEnd"
    - from: "src/agent/react.ts (runAgentLoop)"
      to: "src/llm/stats.ts (getLLMStats/resetLLMStats)"
      via: "STAT 日志块读真实步数统计"
      pattern: "getLLMStats|resetLLMStats"
---

<objective>
闭合 Phase 1 的两条独立能力切片：(1) 废除空游荡强制 speak 兜底（**MEM-03**，让"学习但不推送"成立）；(2) 修 LLM 调用统计恒 0 bug（**MEM-04**，改用 AI SDK v6 `onStepEnd` 按步计数 + generateText 失败重试）。落实 D-05/D-06/D-07（废 speak + 扩空判定 + 进统计不推送）+ D-10（失败重试）+ D-11（onStepEnd 按步计数）。

**垂直切片 1（MEM-03）：** 删一段强制推送代码 + 确认空游荡进 STAT 统计——一条从"游荡结束 → 不再强制推送 → 空游荡仍可观测"的行为切片。
**垂直切片 2（MEM-04）：** stats.ts 重构 + react.ts 回调接线——一条从"generateText 多步 → onStepEnd 回调 → recordStep 累加 → getLLMStats 真实步数"的观测切片。

**关键风险（D-11）：** AI SDK v6 `onStepEnd` 回调内抛错被 SDK 静默吞（Context7 /vercel/ai 已核实 *errors within these callbacks are silently caught*）。计数逻辑必须 try/catch 自愈。Wave 0 须验证 `performance.totalMs` 在 DeepSeek provider 下是否填充（若空 fallback 用 Date.now() 差值——A1）。

**Purpose:** MEM-03 是 Phase 5 推送门控的前提（门控决定推 or 只学，强制 speak 与之矛盾）；MEM-04 让 LLM 成本可观测（现状 calls 恒 1 严重失真）。
**Output:** stats.ts 重构、react.ts 删 speak + onStepEnd 接线 + 重试、Wave 0 测试（含 A1 provider 字段验证）。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-记忆基础设施/01-CONTEXT.md
@.planning/phases/01-记忆基础设施/01-RESEARCH.md
@.planning/phases/01-记忆基础设施/01-PATTERNS.md
@.planning/phases/01-记忆基础设施/01-VALIDATION.md
@src/agent/react.ts
@src/llm/stats.ts
@src/agent/react.test.ts
@src/test/helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stats.ts 重构为 recordStep 按步累加 + Wave 0 测试</name>
  <files>src/llm/stats.ts, src/llm/stats.test.ts</files>

  <read_first>
    - `src/llm/stats.ts:1-54`（现状 LLMStats/CallRecord/startLLMCall/endLLMCall/getLLMStats/resetLLMStats —— 本次整体重构）
    - `src/agent/react.ts:140-141`（resetLLMStats 调用点）、`:182-200`（startLLMCall/endLLMCall 包装 generateText —— Task 2 改造对照）、`:203`（getLLMStats 调用点）
    - `.planning/phases/01-记忆基础设施/01-RESEARCH.md` § Example 1（stats.ts StepRecord + recordStep + getLLMStats 按步聚合）+ Pattern 1（onStepEnd 回调签名）+ Pitfall 1（回调内抛错被静默吞）+ Assumptions A1/A5（performance.totalMs / usage 字段 provider 填充）
    - `.planning/phases/01-记忆基础设施/01-PATTERNS.md` § `src/llm/stats.ts` 段（recordStep API 改造）
    - `src/test/helpers.ts:41-87`（mockChatCompletion / mockFetchError / restoreFetch —— 测试复用）
  </read_first>

  <behavior>
    - `StepRecord` 接口：`{ stepNumber: number; promptTokens?: number; completionTokens?: number; totalTokens?: number; durationMs: number }`（durationMs 可为 0——若 provider 不填 performance，react.ts 的 onStepEnd 用 Date.now() 差值补，A1）。
    - `recordStep(rec: StepRecord): void`：纯内存累加，`steps.push(rec)`；**不抛错**（Pitfall 1：被 onStepEnd 调用，回调内异常被 SDK 静默吞——recordStep 自身必须 no-throw，即使 rec 字段异常也用 try 包裹或默认值兜底内部状态，但绝不向上抛）。
    - `getLLMStats(): LLMStats`：`calls = steps.length`；`totalMs = sum(durationMs)`；`totalTokens = sum(totalTokens ?? 0)`；`avgMs = calls>0 ? round(totalMs/calls) : 0`；返回 `{ calls, totalMs, avgMs, totalTokens }`（LLMStats 接口加 `totalTokens: number` 字段）。
    - `resetLLMStats(): void`：`steps = []`（签名与调用点 react.ts:141 不变）。
    - **移除** `startLLMCall` / `endLLMCall` / `currentCall` / `CallRecord`（不再需要手工配对包装——onStepEnd 驱动）。react.ts 的 start/end 调用由 Task 2 一并删除。
    - 测试（Wave 0，`stats.test.ts`）覆盖：
      1) recordStep 累加：调 recordStep 3 次 → getLLMStats().calls === 3；
      2) 聚合：recordStep({stepNumber:1, totalTokens:30, durationMs:100}) + recordStep({stepNumber:2, totalTokens:50, durationMs:200}) → getLLMStats() = { calls:2, totalMs:300, avgMs:150, totalTokens:80 }；
      3) reset：resetLLMStats() 后 calls === 0；
      4) recordStep no-throw：传 `null as any` 或缺字段 → recordStep 不抛（内部容错）；getLLMStats 不 NaN；
      5) 空状态：未 recordStep 时 getLLMStats() = { calls:0, totalMs:0, avgMs:0, totalTokens:0 }。
  </behavior>

  <action>
    重写 `src/llm/stats.ts`（整体替换，保留文件头注释更新为"按步统计"）：
    - 导出 `interface LLMStats { calls: number; totalMs: number; avgMs: number; totalTokens: number }`（加 totalTokens）；
    - 导出 `interface StepRecord { stepNumber: number; promptTokens?: number; completionTokens?: number; totalTokens?: number; durationMs: number }`；
    - 模块级 `let steps: StepRecord[] = []`；
    - 导出 `function recordStep(rec: StepRecord): void` —— 内部 `try { steps.push({ stepNumber: rec.stepNumber ?? 0, durationMs: rec.durationMs ?? 0, totalTokens: rec.totalTokens ?? 0, promptTokens: rec.promptTokens, completionTokens: rec.completionTokens }); } catch { /* no-throw: 回调内异常被 SDK 静默吞，计数自愈 */ }`（Pitfall 1）；
    - 导出 `function getLLMStats(): LLMStats` —— 按 behavior 聚合；
    - 导出 `function resetLLMStats(): void` —— `steps = []`；
    - **删除** `startLLMCall` / `endLLMCall` / `currentCall` / `CallRecord` interface（grep 确认无其他引用——react.ts 的引用由 Task 2 删除）。

    新建 `src/llm/stats.test.ts`：`describe('LLM stats')` + 按 behavior 5 条断言落地。纯单元测试，无需 mock fetch（stats.ts 无 IO）。
  </action>

  <verify>
    <automated>cd /home/zewang/PROJECTS/cyber-stray && bun test src/llm/stats.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -E 'export (function|interface) (StepRecord|recordStep|getLLMStats|resetLLMStats|LLMStats)' src/llm/stats.ts` 全部命中
    - `grep -c 'startLLMCall\|endLLMCall' src/llm/stats.ts` === 0（旧 API 已移除）
    - `grep -c 'totalTokens' src/llm/stats.ts` ≥ 2（StepRecord 字段 + LLMStats 字段 + 聚合逻辑）
    - `bun test src/llm/stats.test.ts` 退出码 0，5 个子测试全绿
    - recordStep no-throw 测试：`expect(() => recordStep(null as unknown as StepRecord)).not.toThrow()` 命中
    - `bun run typecheck` 通过（注：react.ts 仍引用 startLLMCall 会暂时编译失败——本 task 仅保证 stats.ts 自身可测；Task 2 同步删 react.ts 引用后 typecheck 全绿。本 task 的 `<automated>` 只跑 stats.test.ts 单文件）
  </acceptance_criteria>

  <done>
    stats.ts 重构为 onStepEnd 驱动的按步累加；recordStep no-throw（Pitfall 1 自愈）；5 条单测验证累加/聚合/reset/no-throw/空状态。本任务产出被 Task 2 的 react.ts 回调接线消费。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: react.ts 删强制 speak + onStepEnd 按步计数 + generateText 重试 + config.generateTextMaxRetries 键自包含 + 扩展测试</name>
  <files>src/agent/react.ts, src/agent/react.test.ts, src/types.ts, src/config.ts, data/agent-config.json</files>

  <read_first>
    - `src/agent/react.ts:135-200`（runAgentLoop：resetLLMStats :141 / startLLMCall :182 / generateText :184-194 / catch :195-197 / finally endLLMCall :199）
    - `src/agent/react.ts:202-220`（STAT 日志块 —— speakCount :219 已在，空游荡自动可见，D-07）
    - `src/agent/react.ts:222-229`（**强制 speak 兜底整块 —— 本次删除**，D-05）
    - `src/agent/react.ts:231-246`（recordWanderSummary / appendWanderHistory —— 保留，D-07 数据粒度不丢）
    - `src/agent/react.test.ts:1-44`（既有结构 + useTempDataDir + mockChatCompletion + _resetReactModuleState 复用）
    - `.planning/phases/01-记忆基础设施/01-RESEARCH.md` § Example 2（react.ts onStepEnd 重试循环）+ Example 3（废 speak）+ Pattern 1（onStepEnd 签名）+ Pitfall 1/2（回调吞错 / 失败路径计数）+ Open Question 1（A1 performance.totalMs provider 验证）+ D-10 默认重试 1 次
    - `.planning/phases/01-记忆基础设施/01-PATTERNS.md` § `src/agent/react.ts` 段（废 speak + onStepEnd 计数改造对照）
    - `src/llm/stats.ts`（Task 1 产出：recordStep / getLLMStats / resetLLMStats / StepRecord）
    - `src/types.ts`（AgentConfig 定义 —— 本次加 `generateTextMaxRetries?: number` 字段）
    - `src/config.ts:9-69`（BehaviorConfig Pick 列表 + defaultBehavior + loadBehaviorConfig 缺失回退模式 —— 本次加 `generateTextMaxRetries` 键；**consolidation 键属 01-02，本 plan 不动**）
    - `data/agent-config.json`（既有结构 —— 本次仅追加 `generateTextMaxRetries: 1` 一键；**consolidation 段属 01-02，本 plan 不动**）
  </read_first>

  <behavior>
    - **config.generateTextMaxRetries 键自包含写入（W1 fix）**：本 task 自己定义该键的完整生命周期，不依赖 01-02：
      1. `src/types.ts`：AgentConfig 加 `generateTextMaxRetries?: number`（optional，consolidation 字段属 01-02 不动）；
      2. `src/config.ts`：BehaviorConfig Pick 列表加 `| 'generateTextMaxRetries'`；defaultBehavior 加 `generateTextMaxRetries: 1`（D-10 默认 1 次，RESEARCH 确认）；**不动 consolidation 相关键**（属 01-02）；
      3. `data/agent-config.json`：仅追加 `"generateTextMaxRetries": 1` 一键（保留所有现有键，不动 consolidation 段——consolidation 段由 01-02 Wave 2 后续追加）；
      4. `src/agent/react.ts` 消费 `config.generateTextMaxRetries ?? 1`。
    - **删除强制 speak 兜底**（:222-229 整块）：移除 `if (ctx.spokeTimes === 0 && ctx.visitedUrls.length > 0) { ... await speak(..., 'nonsense') ... }`。空游荡不再推送（D-05）。D-06 空判定扩展：废除后原条件无意义，任何 spokeTimes===0 都是空游荡，STAT 日志块的 speakCount:0 自动体现（D-07，**无需额外代码**——统计已记录）。recordWanderSummary 保留 `'（本次未分享）'` 语义（D-07 数据粒度不丢，RESEARCH Open Question 4）。
    - **onStepEnd 按步计数**（改 :182-200）：
      1. 移除 `startLLMCall()`（:182）与 `finally { endLLMCall() }`（:199）；
      2. `generateText` 调用加 `onStepEnd({ stepNumber, usage, performance })` 回调；回调内 `try { recordStep({ stepNumber, promptTokens: usage?.promptTokens, completionTokens: usage?.completionTokens, totalTokens: usage?.totalTokens, durationMs: performance?.totalMs ?? 0 }); } catch { /* 计数自愈，Pitfall 1 */ }`；
      3. **A1 fallback**：若需 durationMs 精确，在 generateText 前 `const stepStart = Date.now()`，onStepEnd 内 `durationMs: performance?.totalMs ?? (Date.now() - stepStart)`（provider 不填 performance 时用差值）；**Wave 0 测试验证 performance.totalMs 是否填充**（若填充则差值 fallback 永不触发，测试断言 durationMs > 0）。
    - **generateText 失败重试**（D-10）：用 for 循环包 generateText，`maxRetries = config.generateTextMaxRetries ?? 1`（默认 1 次，总 attempts = 2；config 键由本 task 自包含写入，无跨 wave 依赖）；每次 attempt catch 记 `logger.error([traceId] LLM 调用异常 attempt ${i+1}/${maxRetries+1}, { error })`；最后一次仍失败 `ctx.endReason = 'error'`；成功 break。**无指数退避**（RESEARCH Open Question 2：1 次重试无需 backoff）。
    - **STAT 日志块**（:206-220）：llmCalls 读 `getLLMStats().calls`（现已是真实步数）；可选加 `llmTotalTokens`（getLLMStats 现返回 totalTokens）。
    - **D-08（工具失败回喂 LLM）**：本 plan 不改工具，但确认 react.ts:180 `ToolManager.getTools(ctx)` 包装的工具在 execute 抛错时由 AI SDK 原生回喂 LLM（RESEARCH D-08，现状已支持）；在 threat_model 记录此路径为"accept"（非兜底，错误传播）。
    - 测试（扩 react.test.ts）覆盖：
      1) 按步计数 calls>1：mockChatCompletion 返回多步 tool-call 序列（或 mock 让 generateText 触发多个 step）→ runAgentLoop 后 `getLLMStats().calls > 1`（MEM-04 核心）；
      2) 空游荡不推送：mock 让 spokeTimes===0（LLM 不调 speak）→ 断言 speak 未被调（mock speak 或断言 wanderHistory 无 spoke 节点 / 推送历史文件无新增）（D-05/MEM-03）；
      3) 失败重试：mockFetchError → runAgentLoop 后 ctx.endReason === 'error' 且日志可见 2 次 attempt（D-10）；
      4) onStepEnd 自愈：mock onStepEnd 内部抛错（或 mock recordStep 抛）→ 主流程不中断，runAgentLoop 不抛（Pitfall 1）；
      5) durationMs > 0：成功步后 getLLMStats().totalMs > 0（验证 A1：performance.totalMs 或 Date.now() 差值至少一个生效）；
      6) config 键自包含：`config.generateTextMaxRetries === 1`（断言 types.ts/config.ts/agent-config.json 三处一致写入，无 undefined）。
  </behavior>

  <action>
    **先写 config 键三件套（W1 fix，让 01-03 自包含）**：
    - 改 `src/types.ts`：AgentConfig interface 加 `generateTextMaxRetries?: number`（一行，optional；不动其他字段）。
    - 改 `src/config.ts`：BehaviorConfig Pick 列表（:9-30）末尾加 `| 'generateTextMaxRetries'`；defaultBehavior（:32-54）加 `generateTextMaxRetries: 1`（D-10 默认）。**不动 consolidation**（属 01-02）。
    - 改 `data/agent-config.json`：在 `"urlCooldownDays": 5,` 之后追加 `"generateTextMaxRetries": 1,`（仅此一键，保留所有现有键；**不插入 consolidation 段**——该段由 01-02 Wave 2 追加）。用 Edit 精确插入，不破坏 JSON 结构。

    改 `src/agent/react.ts` runAgentLoop：
    - **删 :222-229 强制 speak 整块**（D-05）；保留 :206-220 STAT 块（speakCount:0 已体现空游荡，D-07）；保留 :231-246 recordWanderSummary/appendWanderHistory。
    - **改 :182-200**：移除 `startLLMCall()` 与 `finally { endLLMCall() }`；替换为 D-10 重试循环 + onStepEnd 回调（见 behavior；代码骨架对齐 RESEARCH Example 2 与 PATTERNS § react.ts 段）。import `{ recordStep } from '../llm/stats.js'`（替换 startLLMCall/endLLMCall import）。`const maxRetries = config.generateTextMaxRetries ?? 1`（消费本 task 自包含写入的 config 键，无跨 wave 依赖，**不硬编码、不留 TODO**）。
    - STAT 块 :210 `llmCalls: llmStats.calls` 不变（现已是真实步数）；加 `llmTotalTokens: llmStats.totalTokens`。
    - A1 验证：onStepEnd 内 `durationMs: performance?.totalMs ?? (Date.now() - stepStart)`，stepStart 在每次 attempt 的 generateText 前记。

    扩 `src/agent/react.test.ts`：复用 useTempDataDir/mockChatCompletion/mockFetchError/_resetReactModuleState；按 behavior 6 条断言落地。按步计数测试需 mock 多步 generateText（mockChatCompletion 返回 tool-call 触发多 step，或直接 mock 整个 generateText 模块注入假 onStepEnd 调用序列——**后者更可控**，推荐 mock 'ai' 模块的 generateText 让其在测试中多次调 onStepEnd）。
  </action>

  <verify>
    <automated>cd /home/zewang/PROJECTS/cyber-stray && bun test src/agent/react.test.ts src/llm/stats.test.ts && bun run typecheck && node -e "JSON.parse(require('fs').readFileSync('data/agent-config.json','utf-8')); console.log('config valid')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "nonsense" src/agent/react.ts` === 0（强制 speak 兜底已删，D-05）
    - `grep -c 'onStepEnd' src/agent/react.ts` ≥ 1（按步计数回调接线）
    - `grep -c 'recordStep' src/agent/react.ts` ≥ 1（onStepEnd 内调 recordStep）
    - `grep -cE 'startLLMCall|endLLMCall' src/agent/react.ts` === 0（旧包装已移除）
    - `grep -c 'generateTextMaxRetries' src/agent/react.ts` ≥ 1（D-10 重试读 config）
    - `grep -c 'generateTextMaxRetries' src/types.ts` ≥ 1（W1：types.ts 自包含声明）
    - `grep -c 'generateTextMaxRetries' src/config.ts` ≥ 2（W1：Pick 列表 + defaultBehavior）
    - `grep -c 'generateTextMaxRetries' data/agent-config.json` ≥ 1（W1：agent-config.json 自包含写入）
    - `node -e "JSON.parse(...)"` 校验 agent-config.json 合法
    - `bun test src/agent/react.test.ts` 退出码 0，6 个新增/扩展子测试全绿（含 config 键自包含断言）
    - 按步计数测试：`expect(getLLMStats().calls).toBeGreaterThan(1)` 命中（MEM-04 核心，对齐 ROADMAP Success Criterion #4）
    - 空游荡测试：speak 未被调（断言推送历史/wanderHistory 无 spoke 节点）
    - `bun run typecheck` 全绿（stats.ts + react.ts + config.ts + types.ts 一致，无悬空 startLLMCall 引用）
  </acceptance_criteria>

  <done>
    react.ts 不再强制 speak（MEM-03）；LLM 统计按步累加 calls>1（MEM-04/D-11）；generateText 失败重试（D-10）；onStepEnd 自愈（Pitfall 1）；A1 durationMs fallback 验证。5 条单测全绿，typecheck 通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI SDK generateText → onStepEnd 回调 | 回调内异常被 SDK 静默吞（Pitfall 1） |
| ReAct 工具 execute 抛错 → LLM | 工具失败回喂 LLM 自恢复（D-08，非兜底） |
| DeepSeek API → generateText | 网络错 / API key 错致 generateText reject（D-10 重试） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Repudiation | onStepEnd 回调内异常被 SDK 静默吞 → 计数丢步无日志 | mitigate | recordStep 内部 try/catch no-throw（Pitfall 1）；react.ts onStepEnd 回调外层再 try/catch 双重自愈；统计失真时 at least 不阻断主流程（ASVS V7 可观测性）|
| T-01-13 | Repudiation | LLM 统计 calls 恒 1（现状 bug）致成本不可观测 | mitigate | D-11 onStepEnd 按步计数修复；STAT 日志块输出真实 calls/totalTokens（对齐 ROADMAP Success Criterion #4，ASVS V7）|
| T-01-14 | Denial of Service | generateText 整体失败未重试致单次抖动终结游荡 | mitigate | D-10 按 config.generateTextMaxRetries（默认 1）重试；重试日志可见；最终失败 ctx.endReason='error' 显式（不静默）|
| T-01-15 | Tampering | ReAct 工具 execute 抛错（如 read_page 抓到恶意内容致解析错） | accept | D-08：AI SDK 原生把工具 execute 抛错结构化回喂 LLM 自恢复（非兜底掩盖，是错误传播给决策层）；本 plan 不改工具，仅确认路径（react.ts:180 ToolManager.getTools 已支持）|
| T-01-16 | Information Disclosure | 强制 speak nonsense 推送无关内容打扰用户 | mitigate | D-05：删除强制 speak 兜底（MEM-03）；空游荡进 STAT 不推送（D-07）|
</threat_model>

<verification>
- `bun test src/agent/react.test.ts src/llm/stats.test.ts` 全绿
- `bun run typecheck` 通过
- `bun test`（全量）绿
- 手动：跑一次游荡（mock 或真实），STAT 日志的 llmCalls 反映真实步数（>1 若多步），空游荡无推送
</verification>

<success_criteria>
- MEM-03：react.ts:223-229 强制 speak 块删除，空游荡不推送（对齐 ROADMAP Success Criterion #3）
- MEM-04：LLM 统计 calls > 1（多步 loop 后），不再恒 1（对齐 ROADMAP Success Criterion #4 前半）
- MEM-04：工具错误显式化（D-08 回喂路径确认 + D-09 在 01-01 落地的 getMemory 显式抛错；本 plan threat_model 记录）（对齐 Success Criterion #4 后半）
- D-10 generateText 失败重试落地（默认 1 次，读 config）
- D-11 onStepEnd 按步计数 + 回调自愈（Pitfall 1）落地
- A1（performance.totalMs provider 填充）经 Wave 0 测试验证，fallback Date.now() 差值就绪
</success_criteria>

<output>
Create `.planning/phases/01-记忆基础设施/01-03-SUMMARY.md` when done
</output>

## Artifacts this phase (plan 01-03) produces

| Artifact | Path | Kind | Notes |
|----------|------|------|-------|
| `StepRecord` interface | `src/llm/stats.ts` | interface | onStepEnd 驱动的单步记录 |
| `recordStep` / `getLLMStats` / `resetLLMStats` | `src/llm/stats.ts` | functions | 按步累加 + 聚合 + reset（替换 startLLMCall/endLLMCall） |
| `LLMStats.totalTokens` | `src/llm/stats.ts` | interface field | 新增（成本可观测） |
| runAgentLoop 改造（删 speak + onStepEnd + 重试） | `src/agent/react.ts` | 改造既有 | D-05/D-06/D-07/D-10/D-11 |
| `generateTextMaxRetries` config 键（W1 自包含） | `src/types.ts` + `src/config.ts` + `data/agent-config.json` | config | D-10 默认 1；本 plan 自包含写入，不依赖 01-02（consolidation 键仍属 01-02） |
| Wave 0 测试 | `src/llm/stats.test.ts`（新建）+ `src/agent/react.test.ts`（扩展） | test | 5 + 6 条断言 |
| **移除的符号**（供 source-grounding 排除） | `src/llm/stats.ts` | deleted | `startLLMCall` / `endLLMCall` / `CallRecord` / `currentCall`（不再存在，react.ts 引用同步删除） |

## Deferred（本期不做，注明防 scope creep）

- **D-08 工具失败回喂的显式单测** —— AI SDK 原生行为，本 plan 仅确认路径不改工具；端到端验证留 Phase 5 推送门控
- **onStepStart 实时打点** —— RESEARCH Pattern 1 可选项，本期只用 onStepEnd（拿到 usage/performance 后计数）；onStepStart 留 Phase 6 可观测性
- **重试间指数退避** —— RESEARCH Open Question 2，本期 1 次重试无需 backoff，3+ 次再评估
