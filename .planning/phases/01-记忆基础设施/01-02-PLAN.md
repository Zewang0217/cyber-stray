---
phase: 01-记忆基础设施
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/memory/long-term/archive.ts
  - src/memory/long-term/consolidate.ts
  - src/memory/long-term/consolidate.test.ts
  - src/tools/dedup/url-tracker.ts
  - src/index.ts
  - src/config.ts
  - src/types.ts
  - data/agent-config.json
autonomous: true
requirements: [MEM-02]
tags: [memory, consolidator, soft-delete, mvp]

must_haves:
  truths:
    - "mergeTopicMemories 合并后写新记忆走 store.saveMemory（双写 INDEX.md + .index.json），旧文件 move 到 .archive/knowledge/ 不直接 rm"
    - "cleanupExpired / consolidateOldMemories 的阈值从 data/agent-config.json 的 consolidation 段读取，不硬编码"
    - "consolidator 已接线到 src/index.ts（启动一次性 best-effort 调用，不自动周期触发——D-02）"
    - "cleanupVisitedUrls 已接线（启动一次性调用，参数从 config.consolidation.urlCleanupDays 读）"
    - "每次 cleanup 产 INFO 日志 + 一条 observation 记忆（D-04 双记）"
    - "软删除不可逆数据丢失：旧文件落到 .archive/<type>/，路径过 toSafeFilename 防遍历"
  artifacts:
    - path: "src/memory/long-term/archive.ts"
      provides: "archiveFile(sourcePath, type, basePath) —— rename 到 .archive/<MEMORY_TYPE_PATHS[type]>/"
      exports: ["archiveFile"]
      min_lines: 25
    - path: "src/memory/long-term/consolidate.ts"
      provides: "MemoryConsolidator 改造：mergeTopicMemories 走 store.saveMemory + 软删除、阈值读 config、D-04 双记、cleanupExpired 基于 indexed accessedAt"
      contains: "archiveFile"
    - path: "src/memory/long-term/consolidate.test.ts"
      provides: "MEM-02 Wave 0 测试：merge 走 store / 软删除 / 阈值 / 过期 / observation 双记"
      min_lines: 120
    - path: "data/agent-config.json"
      provides: "consolidation 段（lowImportanceThreshold/expiryDays/mergeMaxAgeDays/urlCleanupDays）+ generateTextMaxRetries"
      contains: "consolidation"
    - path: "data/memory/.archive/"
      provides: "软删除归档目录（knowledge/interactions/observations/profile 子目录按需创建）"
  key_links:
    - from: "src/memory/long-term/consolidate.ts (mergeTopicMemories)"
      to: "src/memory/long-term/index.ts (store.saveMemory)"
      via: "合并记忆走 store 双写（不再 writeFile 绕过索引）"
      pattern: "this\\.store\\.saveMemory"
    - from: "src/memory/long-term/consolidate.ts (mergeTopicMemories/cleanupExpired)"
      to: "src/memory/long-term/archive.ts (archiveFile)"
      via: "rm → rename 到 .archive/<type>/（软删除）"
      pattern: "archiveFile"
    - from: "src/index.ts (main)"
      to: "src/memory/long-term/consolidate.ts (getMemoryConsolidator)"
      via: "启动一次性 best-effort 调用（D-02：不自动周期）"
      pattern: "getMemoryConsolidator|cleanupVisitedUrls"
---

<objective>
接线长期死代码 `MemoryConsolidator` 与 `cleanupVisitedUrls`，让记忆有界能力（合并同话题 / 清理低价值 / 过期遗忘）从"零调用点死代码"变为"可经单测/手动调用验证的活代码路径"。落实 **MEM-02** + D-01（非破坏性遗忘：先合并 + 软删除）+ D-02（本期只接线不自动触发）+ D-03（阈值外置）+ D-04（双记）。

**关键放宽（D-02，已锁）：** 本期只接线 + 单测/手动可跑，**不自动周期触发**。定期调度属 Phase 4 反思周期。本期记忆仍可能无界增长直到 Phase 4——用户已明确接受（ROADMAP Phase 1 Success Criterion #2 措辞已据此更新）。

**垂直切片：** 一条从"合并/清理 → store 双写 → 软删除归档 → 配置阈值 → 启动接线"的端到端能力，落在既有 consolidate.ts/url-tracker.ts/index.ts 上。

**Purpose:** 为 Phase 4 反思回路的周期触发提供已验证的 consolidator；闭合"遗忘是特性"的非破坏性语义。
**Output:** archive.ts 软删除工具、consolidate.ts 三处改造、url-tracker.ts 接线、src/index.ts 启动钩子、agent-config.json 阈值段、Wave 0 测试。
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-记忆基础设施/01-CONTEXT.md
@.planning/phases/01-记忆基础设施/01-RESEARCH.md
@.planning/phases/01-记忆基础设施/01-PATTERNS.md
@.planning/phases/01-记忆基础设施/01-VALIDATION.md
@.planning/phases/01-记忆基础设施/01-01-SUMMARY.md
@src/memory/long-term/consolidate.ts
@src/memory/long-term/types.ts
@src/tools/dedup/url-tracker.ts
@src/index.ts
@src/config.ts
@data/agent-config.json
@src/test/helpers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: archive.ts 软删除工具 + consolidate.ts 三处改造 + Wave 0 测试</name>
  <files>src/memory/long-term/archive.ts, src/memory/long-term/consolidate.ts, src/memory/long-term/consolidate.test.ts</files>

  <read_first>
    - `.planning/phases/01-记忆基础设施/01-PATTERNS.md` § `src/memory/long-term/archive.ts` 段（软删除 pattern + 防遍历 + .archive 不在 MEMORY_TYPE_PATHS 约定）+ § `src/memory/long-term/consolidate.ts` 段（mergeTopicMemories/阈值外置/D-04 双记/死代码接线）
    - `.planning/phases/01-记忆基础设施/01-RESEARCH.md` § Example 5（mergeTopicMemories 改走 store）+ Example 6（阈值外置）+ Pitfall 6（.archive 防误扫）+ Anti-Patterns（绕 store / 直接 rm / 硬编码阈值）
    - `src/memory/long-term/consolidate.ts:1-50`（imports + extractAccessedAt + MemoryConsolidator 构造，注意已 import `writeFile, rm`，本次将 rm 替换为 archiveFile）
    - `src/memory/long-term/consolidate.ts:100-160`（consolidateOldMemories：硬编码 `maxAge = 7*24*60*60*1000, minImportance = 0.3` 在 :108；rm 在 :131；INFO 日志 :147）
    - `src/memory/long-term/consolidate.ts:165-212`（mergeTopicMemories：writeFile 绕 store 在 :200-205；rm 旧文件在 :207-209）
    - `src/memory/long-term/consolidate.ts:217-261`（cleanupExpired：基于 extractAccessedAt + this.config.maxAge；rm 在 :237）
    - `src/memory/long-term/types.ts`（MEMORY_TYPE_PATHS / toSafeFilename / formatMemoryToMarkdown / parseMemoryFrontmatter —— archive.ts 复用）
    - 本 PLAN 依赖的 01-01 产出：`src/memory/long-term/index.ts` MemoryStore.saveMemory（已双写 INDEX.md + .index.json）+ MemoryConfig.consolidation schema（01-01 Task 1 已加 optional 字段）
    - `src/test/helpers.ts:97-115`（useTempDataDir 模式）+ `src/memory/long-term/index.test.ts:1-20`（mkdtempSync + new MemoryStore({basePath:dir}) 结构，测试复用）
  </read_first>

  <behavior>
    - `archiveFile(sourcePath, type, basePath)`：计算 dest = `join(basePath, '.archive', MEMORY_TYPE_PATHS[type], toSafeFilename(basename(sourcePath))) + '.md'`；`mkdir(archiveDir, {recursive:true})`；`rename(sourcePath, dest)`（同文件系统原子 move，RESEARCH Pattern 2）；sourcePath 不存在 → 抛 Error（D-09，不静默跳过）。
    - `mergeTopicMemories(topic)`：
      1. 解析 topicFiles（现状逻辑保留）→ entries；
      2. **构造 merged MemoryEntry 后改走 `await this.store.saveMemory(merged)`**（不再 writeFile(mergedPath) 绕 store——D-01，双写 INDEX.md + .index.json）；
      3. **旧文件软删除**：`for file of topicFiles { await archiveFile(join(dir, file), 'knowledge', this.basePath) }`（不再 rm——D-01）；
      4. `if (!this.store) throw new Error('mergeTopicMemories 需要 MemoryStore 实例')`（D-09 显式报错，不兜底）；
      5. D-04 双记：`logger.info('记忆合并完成', { topic, count, mergedId })` + `await this.store.saveMemory({ type:'observation', tags:['consolidation'], summary:`合并话题 ${topic}`, content:`合并 ${count} 条记忆为 ${mergedId}`, importance:0.3, timestamp:new Date().toISOString() })`。
    - `consolidateOldMemories(options)`：
      1. 阈值从 config 读：`const { lowImportanceThreshold = config.consolidation?.lowImportanceThreshold ?? 0.2, mergeMaxAgeDays = config.consolidation?.mergeMaxAgeDays ?? 7 } = options`（D-03，**消除 :108 硬编码 `7*24*60*60*1000` 与 `0.3`**；保守默认 0.2 / 7 天）；maxAge 内部换算为 ms；
      2. 判定低价值旧记忆后，**rm → archiveFile**（软删除）；
      3. 删后调 `this.store?.deleteMemory` 或直接 jsonIndex.remove（让 .index.json 同步——优先 deleteMemory 以复用双写钩子）；
      4. D-04 双记：INFO + observation 记忆。
    - `cleanupExpired()`：
      1. 阈值 `this.config.maxAge` 改读 `config.consolidation?.expiryDays ?? 60` 天（D-03，**消除硬编码 30 天**，保守默认 60 天）；
      2. accessedAt 来源改为 **indexed accessedAt**（调 `store.jsonIndex.getAccessedAt` 或经 store 暴露的 helper）而非 frontmatter extractAccessedAt（RESEARCH Pitfall 4：accessedAt 已迁索引，01-01 完成）；fallback 用 parseMemoryFrontmatter.timestamp；
      3. **rm → archiveFile**（软删除）；
      4. D-04 双记。
    - 阈值读取模式：沿用 config.ts:59-69 `{ ...defaultBehavior, ...file }` 缺失回退默认；consolidator 构造接收 `config` 引用或在方法内 import `{ config } from '../../config.js'`（后者更解耦——选 import config）。
    - 测试（Wave 0，`consolidate.test.ts`）覆盖：
      1) merge 走 store：mock MemoryStore.saveMemory（或用真实 store 在 mkdtempSync 目录），mergeTopicMemories 后断言 store.saveMemory 被调（merged 记忆 + observation 双记）且 INDEX.md/.index.json 含 merged 条目；
      2) 软删除：merge 后原 topicFiles 在 `.archive/knowledge/` 下存在、原目录不存在；
      3) 阈值读 config：构造低 importance（0.15）+ 旧记忆，consolidateOldMemories 用 config.consolidation.lowImportanceThreshold=0.2 → 命中清理；改 config 为 0.1 → 不清理；
      4) 过期：构造 accessedAt 70 天前（expiryDays=60）的记忆，cleanupExpired → 命中；30 天前 → 不命中；
      5) merge store 缺失抛错：`new MemoryConsolidator(basePath, undefined)` → mergeTopicMemories 抛 Error（D-09）；
      6) D-04 双记：cleanup 后存在 type=observation + tags 含 'consolidation' 的记忆；
      7) .archive 不被重扫：归档后 getRecentMemories 结果不含归档条目（依赖 01-01 的索引只扫 MEMORY_TYPE_PATHS）。
  </behavior>

  <action>
    新建 `src/memory/long-term/archive.ts`（仿 PATTERNS § archive.ts 段）：import `{ rename, mkdir } from 'fs/promises'`、`{ join, basename } from 'path'`、`{ MEMORY_TYPE_PATHS, toSafeFilename, type MemoryType } from './types.js'`、`{ consola } from '../../logger.js'`。导出 `archiveFile(sourcePath: string, type: MemoryType, basePath: string): Promise<void>`，dest 路径用 toSafeFilename 包 basename（防 `../`，RESEARCH Pitfall 6 / ASVS V12）；sourcePath 不存在抛 Error（D-09）。logger = consola.withTag('MemoryArchive')。

    改造 `src/memory/long-term/consolidate.ts`：
    - import 块：移除 `rm` 用法（保留 import 供 archiveFile 不需要——archive.ts 自己 import）；新增 `import { archiveFile } from './archive.js'` + `import { config } from '../../config.js'`（D-03 阈值读取）。
    - `consolidateOldMemories`（:103-160）：:108 改为读 config（`const lowImportanceThreshold = options.minImportance ?? config.consolidation?.lowImportanceThreshold ?? 0.2`；`const maxAgeMs = (options.maxAge ?? config.consolidation?.mergeMaxAgeDays ?? 7) * 24*60*60*1000`）；:131 `await rm(filepath)` → `await archiveFile(filepath, t, this.basePath)`；删后同步索引：`await this.store?.deleteMemory(t, file.replace('.md','')).catch(e => logger.warn('索引同步失败', { file, error: e }))`；:147 INFO 日志扩为 D-04 双记（saveMemory observation）。
    - `mergeTopicMemories`（:165-212）：构造函数校验 `if (!this.store) throw new Error('mergeTopicMemories 需要 MemoryStore 实例（索引双写依赖）')`；:200-205 `await writeFile(mergedPath, ...)` → `await this.store.saveMemory(merged)`；:207-209 `for file { await rm(join(dir,file)) }` → `for file { await archiveFile(join(dir,file), 'knowledge', this.basePath) }`；:211 INFO 后加 D-04 observation 双记。
    - `cleanupExpired`（:217-261）：:218 `this.config.maxAge` → `(config.consolidation?.expiryDays ?? 60) * 24*60*60*1000`；:235 accessedAt 优先读索引（`const indexedAccessedAt = this.store ? await getIndexedAccessedAt(this.store, type, id) : null`——若 01-01 未暴露 helper 则经 store.jsonIndex 或直接 readFile + extractAccessedAt fallback）；:237 `await rm(filepath)` → `await archiveFile(filepath, type, this.basePath)`；:248 INFO 扩 D-04 双记。
    - 删除 `import { rm }` 若无其他引用（grep 确认）。

    Wave 0 测试 `src/memory/long-term/consolidate.test.ts`：`describe('MemoryConsolidator')` + beforeEach mkdtempSync（join(tmpdir(),'consolid-test-')）+ afterEach rmSync；构造 `new MemoryStore({basePath: dir})` + `new MemoryConsolidator(dir, store)`。按 `<behavior>` 7 条断言落地。造数据用 formatMemoryToMarkdown 写 Markdown 文件 + saveMemory 走双写。
  </action>

  <verify>
    <automated>cd /home/zewang/PROJECTS/cyber-stray && bun test src/memory/long-term/consolidate.test.ts src/memory/long-term/archive.test.ts 2>/dev/null || bun test src/memory/long-term/consolidate.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -E 'export (async )?function archiveFile' src/memory/long-term/archive.ts` 命中
    - `grep -c 'await rm(' src/memory/long-term/consolidate.ts` 较改造前减少（mergeTopicMemories/consolidateOldMemories/cleanupExpired 三处 rm 替换为 archiveFile）
    - `grep -c 'this.store.saveMemory' src/memory/long-term/consolidate.ts` ≥ 2（merge 写 merged + observation 双记）
    - `grep -c "config.consolidation" src/memory/long-term/consolidate.ts` ≥ 3（lowImportanceThreshold/mergeMaxAgeDays/expiryDays 三阈值读 config）
    - `bun test src/memory/long-term/consolidate.test.ts` 退出码 0，7 个子测试全绿
    - merge store 缺失测试：`expect(() => consolidator.mergeTopicMemories('x')).toThrow()` 命中
    - 软删除测试：merge 后 `.archive/knowledge/` 下存在原文件、原 `knowledge/` 目录下不存在
    - 无硬编码魔法值：`grep -E '7 \* 24 \* 60|0\.3|30 \* 24' src/memory/long-term/consolidate.ts` 无命中（已外置）
  </acceptance_criteria>

  <done>
    MemoryConsolidator 的 merge/cleanup 路径全部走 store.saveMemory（双写）+ archiveFile（软删除）+ config 阈值；D-04 双记落地；7 条单测验证合并/软删除/阈值/过期/双记/错误显式化/不被重扫。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: agent-config.json 阈值段 + src/index.ts 接线 consolidator/cleanup（启动一次性、不自动周期）</name>
  <files>data/agent-config.json, src/config.ts, src/types.ts, src/index.ts</files>

  <read_first>
    - `data/agent-config.json:29-41`（_urlCooldownNote + urlCooldownDays + feishu 段结构，本次新增 _consolidationNote + consolidation + generateTextMaxRetries）
    - `src/config.ts:9-69`（BehaviorConfig Pick 列表 + defaultBehavior + loadBehaviorConfig 缺失回退模式）
    - `src/index.ts:15-56`（main 函数：validateConfig → initFeishuWS → loadState → startHeartbeat；接线点在 validateConfig 之后、startHeartbeat 之前，或在 startHeartbeat 之后做 best-effort）
    - `src/tools/dedup/url-tracker.ts:179-202`（cleanupVisitedUrls(daysToKeep) 签名，接线时调）
    - `src/memory/long-term/consolidate.ts:265-275`（getMemoryConsolidator(store?) 单例）
    - `.planning/phases/01-记忆基础设施/01-CONTEXT.md` D-02（本期只接线不自动触发）+ D-03（阈值外置）
    - `.planning/phases/01-记忆基础设施/01-RESEARCH.md` § Open Question 3（.archive/ 无界增长留 P4，本期不清归档）
  </read_first>

  <behavior>
    - `data/agent-config.json` 新增（保留所有现有键）：
      - `_consolidationNote` 注释段（说明 D-03 + 保守默认）；
      - `consolidation` 对象（lowImportanceThreshold:0.2 / expiryDays:60 / mergeMaxAgeDays:7 / urlCleanupDays:30）；
      - `generateTextMaxRetries:1`（D-10 默认 1，供 01-03 消费，本 task 先写入避免 01-03 重复改 config）。
    - `src/config.ts`：BehaviorConfig Pick 列表加 `'consolidation' | 'generateTextMaxRetries'`；defaultBehavior 加默认值（与 agent-config.json 一致，作为缺失回退）；AgentConfig 类型（src/types.ts）加对应字段。
    - `src/index.ts` main()：在 startHeartbeat 之前（或 loadState 之后）加 best-effort 接线块：
      - `try { const removed = await cleanupVisitedUrls(config.consolidation.urlCleanupDays); if (removed > 0) logger.info('URL 去重清理', { removed }); } catch (e) { logger.warn('cleanupVisitedUrls 失败（不阻断启动）', { error: e }); }`
      - `try { const consolidator = getMemoryConsolidator(getMemoryStore()); const merged = await consolidator.consolidateOldMemories(); const expired = await consolidator.cleanupExpired(); logger.info('记忆 consolidator 一次性执行', { merged, expired }); } catch (e) { logger.warn('consolidator 一次性执行失败（不阻断启动）', { error: e }); }`
      - **不注册 setInterval / setTimeout 周期触发**（D-02，定期调度属 Phase 4）。
    - 错误处理：接线块用 try/catch best-effort，失败 logger.warn 不阻断启动（consolidator 失败不应让 agent 起不来——这是非兜底的容错，错误被显式记录而非掩盖，符合 D-09 精神：启动接线失败可见）。
    - 测试不在本 task 落（接线点在 main()，集成测试属端到端验证）；本 task 验证靠 typecheck + 启动 dry-run 日志。
  </behavior>

  <action>
    改 `data/agent-config.json`（Edit 整个文件或末尾追加键——用 Edit 在 feishu 段前插入 consolidation 段）：在 `"urlCooldownDays": 5,` 之后、`"_feishuNote"` 之前插入 `_consolidationNote` + `consolidation` + `generateTextMaxRetries` 三键（值见 behavior）。

    改 `src/config.ts`：BehaviorConfig Pick 列表（:9-30）追加 `| 'consolidation' | 'generateTextMaxRetries'`；defaultBehavior（:32-54）加 `consolidation: { lowImportanceThreshold: 0.2, expiryDays: 60, mergeMaxAgeDays: 7, urlCleanupDays: 30 }` 与 `generateTextMaxRetries: 1`；若 `src/types.ts` 的 AgentConfig 缺这些字段则同步加（optional）。

    改 `src/index.ts` main()：在 `registerSignalHandlers()` / `startHeartbeat()` 之前插入 best-effort 接线块（见 behavior）；import `{ getMemoryConsolidator } from './memory/long-term.js'`（或具体子路径）+ `{ getMemoryStore } from './memory/long-term/index.js'` + `{ cleanupVisitedUrls } from './tools/dedup/url-tracker.js'`。**不引入 setInterval/setTimeout**（D-02）。
  </action>

  <verify>
    <automated>cd /home/zewang/PROJECTS/cyber-stray && bun run typecheck && node -e "JSON.parse(require('fs').readFileSync('data/agent-config.json','utf-8')); console.log('config valid')" && grep -c 'consolidation' data/agent-config.json</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '"consolidation"' data/agent-config.json` ≥ 1
    - `grep -c 'generateTextMaxRetries' data/agent-config.json` ≥ 1
    - `node -e "JSON.parse(...)"` 校验 JSON 合法（无语法错）
    - `grep -E 'getMemoryConsolidator|cleanupVisitedUrls' src/index.ts` 两符号均命中（接线落地）
    - `grep -cE 'setInterval|setTimeout' src/index.ts` 较改造前无新增（D-02：无周期触发）
    - `bun run typecheck` 通过（config.ts BehaviorConfig 扩展 + AgentConfig 类型同步）
    - `grep -c 'consolidation' src/config.ts` ≥ 2（Pick 列表 + defaultBehavior）
  </acceptance_criteria>

  <done>
    agent-config.json 含 consolidation 阈值段 + generateTextMaxRetries；config.ts 读取且缺失回退默认；src/index.ts 启动一次性 best-effort 调 consolidator + cleanupVisitedUrls（失败显式 warn 不阻断启动）；无周期触发（D-02）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/memory/*.md → data/memory/.archive/ | 软删除 move（不可逆数据丢失防护） |
| data/agent-config.json → consolidator 阈值 | 配置注入（脏值 / 非法阈值） |
| src/index.ts 启动 → consolidator/cleanup | 启动接线（失败不应阻断 agent） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | data/memory/.archive/ 路径遍历（archiveFile 写入） | mitigate | 复用 types.ts:76 `toSafeFilename`（已防 `../` 与分隔符，现有测试覆盖）；dest 路径 basename 过 toSafeFilename（ASVS V12）|
| T-01-07 | Tampering | .archive/ 被 getRecentMemories/rebuildIndexFromMarkdown 误扫为活记忆 | mitigate | archive 落 `.archive/<MEMORY_TYPE_PATHS>/`，**不在 MEMORY_TYPE_PATHS**；rebuildIndexFromMarkdown 与 getRecentMemories 只扫 MEMORY_TYPE_PATHS 显式四目录（01-01 Task 1 已保证，Pitfall 6 / A3）|
| T-01-08 | Tampering | agent-config.json consolidation 脏值（负数 / 超大阈值致误删或永不清理） | mitigate | Zod 校验 consolidation schema（lowImportanceThreshold 0..1 / expiryDays>0 / mergeMaxAgeDays>0 / urlCleanupDays>0）；缺失字段回退 defaultBehavior（D-03 + config.ts 既有模式）|
| T-01-09 | Repudiation | consolidator 静默清理致数据丢失不可观测 | mitigate | D-04 双记：每次 cleanup INFO 日志（带 topic/count/mergedId）+ saveMemory observation 记忆（tags:['consolidation']），数据形状对齐 Phase 6 可渲染（ASVS V7）|
| T-01-10 | Denial of Service | src/index.ts 启动接线失败阻断 agent 启动 | mitigate | best-effort try/catch + logger.warn 显式记录失败（不阻断 main 流程）；错误可见不掩盖（D-09 精神）|
| T-01-11 | Tampering | mergeTopicMemories 在 store 缺失时静默跳过（现状 bug 类） | mitigate | `if (!this.store) throw new Error(...)`（D-09 显式报错，不兜底）|
</threat_model>

<verification>
- `bun test src/memory/long-term/consolidate.test.ts` 全绿（7 条断言）
- `bun run typecheck` 通过
- `bun test`（全量）绿
- 手动：`bun run dev` 启动后日志含"记忆 consolidator 一次性执行"或 cleanup 失败的 warn（接线可见）
- 手动：检查 data/memory/.archive/ 目录在 cleanup 后存在归档文件（软删除可见）
</verification>

<success_criteria>
- MEM-02 全部 4 条 Validation Architecture 测试映射（merge 走 store / 软删除 / 阈值 / 过期）单测全绿
- mergeTopicMemories 不再 writeFile 绕 store（D-01）；rm 全部替换为 archiveFile 软删除（D-01）
- 阈值（lowImportanceThreshold/expiryDays/mergeMaxAgeDays/urlCleanupDays）从 config 读取，无硬编码（D-03）
- consolidator + cleanupVisitedUrls 已接线 src/index.ts（启动一次性、不周期，D-02）
- ROADMAP Phase 1 Success Criterion #2 放宽口径达成（"接线 + 单测/手动可跑"，非"周期自动执行"）
</success_criteria>

<output>
Create `.planning/phases/01-记忆基础设施/01-02-SUMMARY.md` when done
</output>

## Artifacts this phase (plan 01-02) produces

| Artifact | Path | Kind | Notes |
|----------|------|------|-------|
| `archiveFile` | `src/memory/long-term/archive.ts` | function | 软删除 rename 到 .archive/；toSafeFilename 防遍历 |
| `MemoryConsolidator` 改造（mergeTopicMemories/consolidateOldMemories/cleanupExpired） | `src/memory/long-term/consolidate.ts` | 改造既有 | 走 store.saveMemory + archiveFile + config 阈值 + D-04 双记 |
| `consolidation` config 段 | `data/agent-config.json` | config | lowImportanceThreshold/expiryDays/mergeMaxAgeDays/urlCleanupDays（D-03）|
| `generateTextMaxRetries` | `data/agent-config.json` + `src/config.ts` | config | D-10 默认 1（本 plan 先写入，供 01-03 消费） |
| `BehaviorConfig` 扩展（consolidation/generateTextMaxRetries Pick + default） | `src/config.ts` | 改造既有 | 缺失字段回退默认 |
| `data/memory/.archive/<type>/` | 数据产物 | directory | 软删除归档目录（按需 mkdir） |
| 启动接线块 | `src/index.ts` main() | 改造既有 | 一次性 best-effort consolidator + cleanupVisitedUrls（D-02 不周期） |
| Wave 0 测试 | `src/memory/long-term/consolidate.test.ts` | test | 7 条断言 |

## Deferred（本期不做，注明防 scope creep）

- **consolidator 周期/定时自动调度** —— Phase 4 反思周期接管（D-02，ROADMAP #2 已放宽）
- **.archive/ 定期清理** —— 本身会累积，清理留 Phase 4（RESEARCH Open Question 3）
- **游荡节点轨迹 TUI/仪表盘 UI** —— Phase 6（OBS）
