# CONTEXT — cyber-stray SaaS 共享领域语言

> 用途：多会话/多 agent 协作时的共享词汇与已锁定决策。只记**决策与非显而易见**的，不复制 CLAUDE.md/spec 已有内容。
> 来源：issue #42 RFC 讨论 + 调研（企业级 SaaS / 独立 SaaS / 多租户架构）。目标部署：单台 2 CPU / 4 GB VPS，systemd process-per-tenant 起步。

## 词汇（一个词一个义）

| 术语 | 含义 |
|---|---|
| **租户 tenant** | 付费/注册单位（≈组织 org）。**计费挂租户**不挂宠物。可养多只宠物。 |
| **宠物 pet** | 一只独立 agent 实例：独立 state / 记忆 / 兴趣 / 推送。核心价值（闭环自进化+主动推送）作用在宠物层。 |
| **控制面 control plane** | 共享（pooled）服务：认证 / 计费 / Web 应用 / 飞书·TG 推送网关。无聊的东西共享。 |
| **伴侣端 companion** | 主人交互面：首选 **PWA 网站**（一套代码覆盖移动+桌面，Web Push 推送）。 |
| **通道 channel** | 推送后端（飞书 / Telegram）。从"全局单一目标"改为"每租户绑定"，可选。 |
| **数据目录 data dir** | 租户隔离的 markdown 持久化单元（`DATA_DIR`）。= markdown 世界的 "database-per-tenant"。 |
| **宠物 IP 资产 pet asset** | 宠物视觉素材集（角色参考图 + 像素 sprite + manifest），租户私有；免费用户用平台预置系列，Pro/BYOK 可自助生成。 |
| **像素 sprite** | 宠物的多帧像素动画资产：`cat.png`（spritesheet 横排）+ `frames.json`（帧表 manifest v2）；32×32 网格、脚底接触行 r30、14 色 ∪ 猫本体色板（design-v3/motion.md §3 契约）。 |
| **锚点帧 base frame** | 一个动作的基准单帧：角色的视觉真相源；其余帧由程序派生或生图参考它，不独立生成。 |
| **程序派生 derived frames** | 从锚点帧按骨架相位规则（腿相位/尾摆/下压/腾空）确定性变换出的动画帧；帧间一致性由同源派生保证，不赌生成模型。 |
| **毛色重映射 coat remap** | 对像素 sprite 的毛色系（灰阶/主色/纹色）做确定性色板替换生成毛色变体（橘/黑/三花，DESIGN.md §7 图鉴皮肤）；零生成成本。 |
| **性格 personality** | 宠物行为/语气/日记风格的可拓展注册表（好奇/活泼/慵懒/沉稳…），认领时选择；每性格一组独特优劣 + 参数倍率 + 语气段。 |
| **口头禅 catchphrases** | 宠物说话标识：带权重集合 `[{text, weight}]`（默认均匀 3-4 条，纯文字骨架不带 emoji）；每性格注册表有默认组，认领时可改、设置页可编辑；权重由反馈归因低频进化（A 起步→C，下限 0.05）。 |
| **领养仪式 adoption ritual** | 认领流程 4 步：起名 → 选性格 → 口头禅 → 选兴趣；起名/口头禅步 LLM 一次返回 3 候选（解析拆分），"换一批"每步限 3 次；性格步纯自选（4 卡，无 AI 推荐）；交互文案营造"领养宠物"感。 |
| **作息 sleep schedule** | 用户可设定的宠物睡眠时间段（同 pushWindow 字段模式）；睡眠期调度器跳过游荡，前端展示睡觉。 |
| **日记 diary** | 宠物每日叙事记录（睡前预生成，性格化语气，多风格选项，可定时推送），`diary/YYYY-MM-DD.md`。 |
| **梦境 dream** | 宠物睡眠期间的抽象叙事（睡前预生成，夜间访问查看），与日记同刻生成但独立文件。 |
| **表情包 meme** | 宠物生成的梗图：画面（生图）+ 梗文字（程序叠加，LLM 出文案）；两触发点（日记写完 / 推送后 agent 调 tool）。 |
| **表情包图鉴 meme gallery** | 收录过质检表情包的页面，每张带元数据（话题/情绪/日期），支持下载/删除。 |
| **内容生成管线 image pipeline** | AI 生图/程序渲染 → 确定性后处理（pixelize：下采样/色板量化/基线对齐）→ 三层质检 → 成品落租户目录的全自动链路；宠物 sprite 与表情包共用。 |
| **生图模型 image model** | 火山方舟 Seedream 5.0 Lite（ADR-0006 替代原 DashScope wanx）；全局配置化，admin 面板可更换。 |
| **视觉质检 vision QC** | 生成图语义质检（状态正确/角色一致/无水印畸形），智谱 GLM-4V-Flash（免费档；ADR-0006 正文「豆包视觉」与代码不符，以代码为准勘误）。 |
| **用量 usage** | 内部成本计量：每次 LLM 调用（token）+ 每次生图/质检（张数），按单价表折算费用；落租户 usage JSONL（ADR-0007）。 |
| **单价表 price table** | 默认单价配置化（Seedream/豆包视觉/DeepSeek），admin 可覆盖；不接外部价格 API。 |
| **微信通道 wechat channel** | 基于腾讯官方 iLink Bot API 的通道（2026 开放，合规无封号）；双向 DM 互动 + 受限主动推送；获客定位。 |
| **推送门控 push gate** | speak 前的价值判断机制。P3 重构后 = **反馈抽样器**（决定哪些 speak 暴露给主人以获得反馈信号），不再做内容评分拦截；防骚扰靠预算层（S11）+ 工具层护栏（去重/上限）。 |
| **LLM 自判断 speak** | 游荡循环内 LLM 自行决定是否调用 speak（可保持沉默），不用评分公式；prompt 注入双图谱上下文 + 剩余日预算。 |
| **用户兴趣图谱 user interest graph** | 主人的兴趣模型（话题+权重），驱动推送"相关性"；来源 = 领养仪式种子 + 反馈（like/boost/dislike）+ 隐式信号；与宠物好奇图谱分离。 |
| **宠物好奇图谱 pet curiosity graph** | 宠物自身的探索方向模型（话题+权重），驱动"新奇度"；由游荡内容提取/反思演化；保证宠物的自发探索与开放思维。 |

## 已锁定决策（讨论中确认，勿擅改）

### 商业模式
- **双轨定价**：免费档 · Pro 固定月费 · BYOK 重用户自带 key（可仿 OpenRouter 抽 ~5%）。
- **不建自建计量系统**。DeepSeek token 是唯一真实 COGS；计量等规模上来再用 Stripe Billing Meters。
  - **内部成本可视化不冲突**（ADR-0007，2026-08-25）：运营看板统计每宠物 LLM token/生图/费用（usage JSONL + admin「用量」tab），是内部成本可视化，**不对外计费**；对外计费仍是 Stripe 后续路。
- **三档边界（已定）**：
  - 免费：推送 3–5 次/天，兴趣方向操控 **1 次/月**（**自进化始终免费**，宠物自己长兴趣不受限）。
  - Pro（建议 $3–5/月）：推送 ≤20 次/天，**可自定义推送时间**，兴趣方向操控 **1 次/天**。
  - BYOK：自带 DeepSeek key，不限（平台不烧 token）；操控≥Pro。
  - **多宠物尚未实现**——两档当前都 1 宠物；多宠物是后续版本钩子。
  - 区分框架 = 卡**主人手动操控**频率，不卡宠物自进化（勿把"自进化"做成付费墙，那是核心价值）。

### 多租户服务端
- **Bridge 隔离**：控制面 pooled + agent 运行时/存储 siloed。
- **调度器 + 短命 worker（宠物执行模型）**：宠物**不是常驻进程**。调度器扫控制面 SQLite，按"无聊/精力就绪"（由 `lastRunAt` + 流逝时间前推算，无需常驻进程）拉起短命 Bun worker，载入该宠物 markdown 记忆、跑一小段游荡、写回并更新 DB 状态、退出。任何时刻一宠物一 worker（隔离不变）。几百个注册宠物只有"正无聊"的几只醒着 → 一台 2h4g 就够。
  - 触发 = 无聊/精力（time-propagable）；方向 = 兴趣（InterestGraph focusTopics）；进化 = 反思 + 反馈。
  - **容器化部署（ADR-0008，2026-08-26 取代本条旧决策）**：生产 = GHCR 镜像 + compose（控制面+agent 同镜像、web、casdoor 官方镜像），产机只跑容器不再当 runner。旧「不是 Docker 起步」针对的是横向扩展，不适用于不可变交付与产机角色分离。
- **不迁 Postgres**：文件系统 markdown 持久化让租户隔离近乎免费；租户备份 = `tar` 一个目录。
- **租户解析**：JWT org claim 为准 + 路径前缀 `/t/<slug>`。不做子域名（单机要通配 DNS+证书，性价比低）。
- **安全硬规矩**：服务端剥掉客户端传入的 tenant header；每处数据访问带已验证 tenant id。
- **每租户 secrets**：信封加密，master key 包每租户 DEK，DEK 加密存租户目录（并存用户 key / 平台 key）。不用 Secrets Manager / Vault。
- **状态在盘**：worker 无状态可杀可拉；`getDataPath()` 按 `DATA_DIR` 即租户键。
- **鉴权 = Casdoor**（Apache 2.0，自托管）：官方镜像跑在 compose 内（ADR-0008 前是裸二进制 + systemd unit），SQLite 嵌入式账号库（用户/组织/角色，零数据库依赖）。多租户原生的 organization 模型。
- **控制面元数据存储 = SQLite + ORM**（推荐 Drizzle/Prisma）：用户↔租户↔宠物↔账单等关系数据。**不上 Postgres 实例起步**；`SQLite↔Postgres` 靠 ORM 只需改连接串。触发切 Postgres 的**命名门槛 = 需多机共享控制面库**（SQLite 单节点不能多机写），非"客户变多"。

> 澄清：宠物数据仍是每租户 markdown 目录（不迁任何 SQL）。"控制面 SQLite"是另一层（注册/租户/计费/inventory），不违反记忆保持 markdown 约束。SQLite 本身即关系数据库（SQL/事务/索引），嵌入式 vs 服务器版才是真对比。

### 客户端
- **一套 PWA web**，租户上下文来自 JWT（前端状态是便捷，服务端 claim 是真相）。
- 主题 token + `plan` 门控启动时从 pooled API 拉取。
- **保留飞书/TG**（不破坏既有契约），降为可选通道；PWA 是首选伴侣面。
- **Dashboard 内容（已定）**：核心使命 = 让"自进化闭环"可见（区别于纯推送机器人）。
  - Slice 1：宠物主页（心情/精力/无聊值）+ 推送动态流（含推送理由）+ **内联反馈入口**（闭环必需项）。
  - Slice 2：兴趣图谱可视化 + 游荡足迹（数据可视化取向 A，用现有 interests/wander-history 数据）。
  - 后续方向 B：品牌 IP / 宠物拟人化（小动画/像素宠物）/ 美术素材采集创作——锦上添花，非首期。
  - 后续方向：**性格系统**——用户可选宠物性格（如好奇/慵懒/活泼），映射到行为参数（boredomGrowth/energyCost/explorationMode 倾向）。**非首期**。

### 用户旅程（已定）
```
注册(Casdoor, 首登自动建租户) → 领养宠物(4 步仪式：起名/性格/口头禅/兴趣, 起名+口头禅步 LLM 3 候选可换批限 3 次, 各步可编辑)
→ 宠物自我介绍(UI 展示) → 首推(PWA) → 反馈(点赞/踩/顶话题, 闭环) → 日常按 plan 频率
```
- 推送通道：**默认 PWA**；飞书可选（高级用户）。
- 出发点：让"自进化闭环"可见；免费用户体验"宠物自主进化"，Pro 得到"可操控"。

### 实时形态（已定）
- **两层实时**：应用内实时 = **SSE**（单向，替代 5s 轮询）；系统级推送 = **Web Push**（Service Worker，App 关了也收）。
- 选 SSE 不选 WebSocket：实时流几乎全单向（状态/推送），WebSocket 双工是给聊天/协同用，客户端动作（反馈）走 REST POST。
- SSE 租户安全：每条连接建立时绑定 tenant id（JWT），广播只推该租户连接，不跨租户泄漏。
- **无 Redis**：单机用进程内事件总线（调度器嵌入控制面进程或轻量本地 IPC）。Redis pub/sub = 多机共享触发点才上（与 Postgres 同一触发点）。
- 兜底：SSE 不稳时降级轮询。

### 反馈回路（已定）
- 首版 = **B：点赞/踩（👍👎）+ 顶话题（显式"我要更多这个方向"）**，走 REST POST。
  - 👍/👎：低价值高频信号，**不受批次节流**。
  - 顶话题：走"兴趣方向操控"节流（免费 1/月，Pro 1/天）。
- **轻交互 pet-interaction**（新术语，区别于反馈）：用户对**宠物本身**的状态表达（如拍拍/夸一句/回应心情），只影响 mood/语气，不影响 boredom/energy/temper/生存/自进化。与反馈同构路径（Web POST → 控制面 → agent worker 异步消化），但语义不同：反馈评价推送内容，轻交互表达对宠物的态度。不破坏 Web 只读契约。区别于 Tamagotchi 照料（照料会衰减生存，赛博宠物不会）。
- **反馈→口头禅归因**：主人对推送点赞/踩时，除兴趣归因外，同管线把权重归因到该条 speak 用过的口头禅（A 起步；反思自我调节记后续）。权重下限 0.05。口头禅演化历史落盘存储（暂不展示，可扩展）。

### 内容生成与宠物生命周期（grill 2026-08-19 设计，进 issue 拆分）
- **宠物 IP 自定义**：免费 = 平台预置 IP 系列；Pro/BYOK = 自助全自动生成（配额，如 2 套/月），**租户私有**。spec = 纯文本 + 选项（允许抽象/自由风格，宠物素材与图鉴整体风格可轻微违和）。流程两阶段：spec → workflow 内部 prompt 优化 → 角色概念图 → **用户确认/调整** → 参考图 → 多状态生成 → 切分 → 成品。
- **宠物 sprite 混合管线**（#169 落锤 2026-09-06，取代旧「单图多状态 + 每状态 1 帧」决策）：**B 基混合**——锚点帧（base）由生图（Seedream 像素纪律 prompt + img2img）或 LLM 字符网格自绘产出，动画帧一律**程序派生**（腿相位/尾摆/下压，同源保证帧间一致），公共后处理 = 确定性 pixelize（alpha 腐蚀 → 32×32 降采样 → 17 色硬量化 → 底中基线对齐）+ 三层质检。多帧 spritesheet（`cat.png` + `frames.json` manifest v2）取代"每状态 1 帧 + 播放器微动画"。依据：#168 A/B 原型——B 表现力胜出（持机人初判），A 一致性/成本结构性占优，混合各取所长；A 纯自绘为零成本保底路径（渲染器可单测，不赌外部服务）。B 模型侧冒烟待 ARK key，未实证前默认猫用 A 侧预生成资产。
- **生图统一契约**：图生图参考锁角色（火山方舟 Seedream 参考模式——原「qwen-image 参考模式」已过时；复用锚点帧批量生成，无需训 LoRA）；**图文分离**（生图只画画面，梗文字 LLM 出文案 + 程序叠加——调研确认行业压倒性主流，AI 画整句中文文字不可靠，Glyph-ByT5 基线 <20%）。meme 宠物主角一致性 = 把宠物像素 sprite 锚点帧（白底放大版，走既有 `--flatten` 链路）作参考图喂生图。
- **三层质检**（取代旧「两层质检」）：结构层（脚本：尺寸/透明底/内容占比 + 像素四项：色板出界=0/假像素=0/基线一致/网格合规）+ 语义层（GLM-4V-Flash，参考图换成锚点帧）+ **程序层**（dHash 感知哈希帧间漂移检测，零 API 成本前置拦截）；失败 = 单帧重试，N 次不过 → 返回用户改 spec。
- **存储/交付**：成品落 `data/tenants/<sub>/pet-assets/`（与记忆同根，备份天然包含）；CP 鉴权静态路由服务（session claim 校验租户）；web 按租户 manifest，无自定义回退内置素材。web 保持只读契约。
- **性格系统**（本期做，前置依赖）：可拓展注册表（好奇/活泼/慵懒/沉稳，每性格独特优劣），认领时选择，前端展示；影响 boredomPerMinute/energyPerMinute 倍率、READY 阈值偏移、游荡效果、探索倾向（新/旧话题权重）、语气 prompt、日记/梦境风格。细节进 issue。
- **真实作息**：宠物有用户可配睡眠时间段（睡眠期调度器跳过游荡）；**睡眠期轻互动不醒**（拍拍 → 翻身/哼唧动画回应，梦境不打断，不改真实参数）；夜间访问展示睡觉状态 + 梦境。
- **日记 + 梦境两个功能**：睡眠开始同刻预生成——日记 = 当天事实 + 性格化叙述（游荡足迹/新兴趣/主人反馈），梦境 = 抽象叙事（兴趣的联想重构，openclaw 式活物感）；日记多风格选项 + 可定时推送；梦境夜间访问查看。记忆巩固（openclaw 真记忆压缩）= backlog，看情况再上。
- **表情包两触发点**：日记写完生成（日记配图/相关梗）+ 推送后 agent 调 tool 生成（agent 自主判断话题适合做表情包）；**推送补发带图**（飞书/TG 原生图片，web 附链接）；图鉴自动收录过质检的，带元数据（话题/情绪/日期），支持下载/删除；参照 Memedex/MemeOrganizer 模式（标签 + 检索，CLIP 语义检索为升级路径）。
- **差异化机会**（调研结论）："宠物 agent 自动生成专属表情包并收录"是行业空白，无竞品。

### 推送门控与自进化（grill 2026-08-31，P3 重构）
- **门控重构 = 反馈抽样器**（替代评分防火墙）：speak 由 LLM 自判断（内联进 ReAct，可保持沉默），无评分公式。防骚扰 = S11 日预算（**会员等级 + 用户设置**决定）+ 工具层护栏（去重/每游荡上限）。双图谱上下文 + **剩余日预算进 prompt**（LLM 自己权衡"这条值不值得用掉配额"）。
- **话痨可接受**：不靠内容评分压制，靠预算 + 代码控制；预算感知进上下文。
- **双图谱（已定稿，grill 2026-08-31）**：同一 taxonomy 骨架（2-3 级，根=领养仪式兴趣），两个文件各自记录语义：
  - **用户兴趣图谱** `user-interests.json`：主人偏好，叶子带 weight。多信号融合：like/boost/dislike + 未来访问频率（**独立待办：推送点击埋点**）；更新 = 半衰期衰减(60天) + Σ信号×强度×阻尼(1/(1+0.2n))×(1−weight)（饱和增长+边际递减，防单话题压制）；dislike 只落叶子不碰父级；prompt 注入按强/中/弱分级展示。
  - **宠物好奇图谱** `curiosity-interests.json`：宠物自己的品味。exploreCount（游荡被动计数）+ **selfInterest**（反思时 LLM 自我判断"我是否感兴趣"，依据=性格 novelty/familiarity 参数 + 记忆积累；先看效果，不好再移时机）。高 selfInterest → 说话更热情、更愿深挖；低 → 平淡带过；影响宠物说话方式与内容。
  - **分类机制**：复用 DeepSeek（不引小模型——频率低、零基建、分类与生成同一世界观）。三触发点：speak 定稿后分类存 contentTopics（+1 调用/推送）；反馈按 contentTopics 精确归因叶子；反思时批量分类 + 自我兴趣判断。
  - 用户图谱 = 主人想看什么（相关性）；好奇图谱 = 宠物想探索什么（品味）；共同进 prompt，LLM 自判断权衡。
- **进化要渐进（gradual）**：图谱/画像变化平滑、不突变；防单点强化与熵坍缩。
- **用户画像 = 目录容器（模块化，单写者，摘要派生）**：`user-profile/` 内含 `identity.json`（基本信息，低频写）+ `settings.json`（推送偏好/通道路由）+ `user-interests.json`（用户兴趣图谱，唯一写者=进化循环）+ `profile-summary.md`（**派生**叙述摘要，反思时由 DeepSeek 从图谱重新生成，绝不独立维护）。模块间单写者纪律 = 防漂移（Mem0/OpenClaw 模式）。
- **宠物侧独立于用户**：`curiosity-interests.json`（宠物品味）+ `memory/`（宠物记忆）归宠物，`user-profile/` 归主人，边界清晰。
- **探索-利用平衡（先 A，B 后备）**：A = 纯 prompt 引导（好奇图谱高新奇话题在 speak 判断 prompt 单独高亮 + 分享准则给探索"许可"），先看效果；B = 代码预留探索槽（日预算内固定比例强制来自好奇图谱）效果不好再加；UCB/ε-greedy（Netflix MAB 模式）= **低优先级待办/issue**，按需再写。
- **skill 进化共享 = 低优先级 issue #148（延后，先建 eval 系统）**：宠物解决新问题 → 沉淀 skill → 跨宠物共享（beta）。**前置依赖 = eval 系统**（否则无法判断"进化有效/值得沉淀"，防自确认）。设计草案：触发=自动入库+执行验证（Voyager），存储=全局注册表+provenance，共享=默认关+opt-in+管理员审核。毒化风险严重（SKILL.md 语义投毒 ~80% 劫持），按不可信代码对待。
- **待做（不进本期实施）**：① prompt cache 优化（动态信息位置、system message 打磨）② 去重升级——现只按 URL，待加 BM25/语义相似度拦截。
- **生产事故根因**（issue #147）：E2E 测试数据污染画像（单条 dislike 写死整个话题）+ 兴趣分公式倒挂（命中头号兴趣反而扣分）+ 租户 worker 反思从不运行（反思调度器只在 StrayHarness 初始化，WanderAgent 路径缺失）。

### 微信通道（grill 2026-08-19，调研后定，等 iLink 实现调研回填）
- **定位**：获客通道 + 双向互动主体验。微信 DM 是互动入口（主人就在自己微信聊天框跟宠物说话）；主动推送辅助（受 iLink 额度限制）。飞书/TG/PWA 保持主力，微信为第四通道（可选绑定，复用 channel 模型）。
- **通道技术**：腾讯官方 **iLink Bot API**（ilinkai.weixin.qq.com，2026 开放 = 微信 ClawBot 背后 API）。逆向方案（wechaty+padlocal / wechatferry / itchat）全线衰退 + 封号风险，**一律不做**。
- **官方硬约束（issue #202/#142 实测确认）**：主动推送 ≈10 条/24h 有效会话（超量 ret=-2）；24h 无交互 context_token 失效；需主人先发消息建立会话；群消息不可用（仅 DM）。扩额诉求 #202 官方已受理未落地（2026-08-18 仍 open）。
- **onboarding = 扫码即用（微信即账号）**：主人扫码绑定 → 自动建租户 + 领养默认宠物 + 免费档起步 → 微信聊天框直接用；web 登录为可选升级（绑定后并入 Casdoor 账号体系解锁 Pro/自定义）。微信身份是租户锚点，认证边界扩展（Casdoor 之外的新入口），计费/配额/数据目录复用现有模型。
- **每租户一个 bot（BYO）**：用户自己扫码操作，平台做指引（web 绑定页流程指引 + 验证）；不要求用户跑任何服务；每租户独立 bot 身份/登录态/24h 保鲜。
- **激活机制**：检测到主人先发一条消息 = 激活；激活前宠物只发打招呼提示，不推内容。token 24h 保鲜：超时需主人重新打招呼，web 端提示"微信通道已过期，发条消息重新激活"。
- **通道限额**：微信通道推送上限 = `min(套餐, 8 条/天)`（10 条上限留 20% 余量）；超限推送自动降级走其他已绑通道（微信优先，超限降级）。
- **实现**：自研轻量 iLink 适配器（参考 hermes-agent weixin.py / openclaw-weixin），CP 侧通道驱动（长轮询收消息 + 主动推送 + 保鲜管理），复用 channel 抽象；不引入 hermes-agent 整体。实现级调研已回（ilink-research，2026-08-19）：
  - **协议面积极小**：9 个 HTTP 端点、无 WebSocket（基座 ilinkai.weixin.qq.com）；官方 Tencent/openclaw-weixin（TS 源码全模块+单测）最权威参考；TS SDK 生态 @wechatbot/wechatbot（606★）、@weixin-ts/bot（Bun 原生兼容）可兜底。
  - **扫码绑定无预注册**：`get_bot_qrcode` 取二维码 → 长轮询 `get_qrcode_status` → confirmed 返回 `bot_token` + `ilink_bot_id`（租户 bot 身份）+ `ilink_user_id`（扫码主人微信 ID，pairing 白名单用）+ baseurl。
  - **多租户模式已验证**：每账号一条独立长轮询循环 + 按账号持久化游标/context_token；corespeed SDK 已跑通"每租户实例 + QR 回调推 Web UI 扫码"；hermes 实测 60 bot 并发无压力（2C4G 够）。
  - **ret=-2 多义**：`errmsg=='unknown error'` = 会话失效（需重新激活），否则按限流 3× 指数退避。
  - **分块**：4000 字自然边界（hermes 用 2000 保守）；首版只做文本推送，媒体 CDN 需国内出口（服务器在国内 = 可加图，表情包图片等 #96 落地后接）。

### 认证架构（ADR-0004，2026-08-20 重构）
- **Casdoor 组织 = 产品/项目域**：一个产品一个组织 + 一个应用；业务用户注册到产品组织（普通用户）；未来新项目 = 新组织 + 新应用，天然隔离。
- **built-in 组织** = Casdoor 全局管理员域，禁注册（安全设计），不承载业务用户。
- **租户隔离在 CP 层**（user_tenants 复合主键，租户键 = Casdoor sub）；Casdoor 组织不参与业务路由（认证管"你是谁"，CP 管"你能访问哪个租户"）。
- **admin 分两种**：Casdoor 全局管理员（built-in 身份）vs 产品管理员（CP 层 `admins` 表 ∪ `CP_ADMIN_SUBS` env 白名单）；产品管理员判定不迁 Casdoor role（admins 表 RBAC 已够）。
- **注册 = 用户名 + 显示名 + 密码**（邮箱/手机选填，字段保留）；验证码后续；防滥用 = nginx 层限流（独立安全任务）。
- **注册入口**：应用登录页 sign up 链接（`/signup/oauth/authorize?client_id=...`）；裸 `/signup` 走默认应用（built-in，禁注册）——勿直接访问。

## 待定（拍板前别写实现）
1. 向量库：暂不做，后续看检索是否需要（可加 SQLite 检索索引）。

> 记忆内容**保持 markdown**（人可读/可调试），不整体迁 SQLite（体量大）。编排状态进 SQLite；记忆可加 SQLite 检索索引（后续按需）。

## 硬约束（继承，勿违背）
- Bun/tsx + AI SDK v6 + DeepSeek + 文件系统 markdown 持久化。
- 记忆层保留人类可读 Markdown，不整体迁 SQLite。
- 飞书/TG 推送、TUI、Web 只读契约不可破坏。

### 术语澄清：赛博 = 云/在线（非赛博朋克）
- 产品语境里「赛博宠物」的**赛博** = **云/在线**（cloud-native, 一直在线的后台 agent），不是 cyberpunk 赛博朋克。
- 但项目名「Cyber Stray / 赛博街溜子」的「Cyber/赛博」在历史语境里指 cyberpunk/cyber culture（旧 DESIGN.md Cyber-Fluid 即此义，已作反参考废弃）。
- **视觉决策以此为准**（2026-09-06 更新）：视觉世界 = **design-v3「像素街区 · STRAY-BOY」**（design-v3/ 四文档 + demo.html 为世界宪法与动效验收基准）。旧「维多利亚自然博物图鉴」视觉基准已废弃，不再作为任何 UI 决策依据。「赛博」的语义不变：**云/在线**（cloud-native、一直在线的后台 agent、实时同步），不是 cyberpunk 质感——像素夜城承载的是"云端活物"，霓虹/掌机是表现语法而非科技感来源。
